import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.Paisa',
  appName: 'Paisa',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
